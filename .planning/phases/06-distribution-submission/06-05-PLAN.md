---
plan: 06-05-public-and-marketplace-test
phase: 06
wave: 3
type: execute
autonomous: false
depends_on: [06-01-readme-hero, 06-04-npm-package]
requirements: [DIST-05]
files_modified:
  - docs/marketplace-test.md
task_count: 2
objective: Make the GitHub repo public and document the `/plugin marketplace add` verification flow for the user to execute in their Claude Code session.
must_haves:
  truths:
    - "Repo `kocaemre/zama-skills` is public on GitHub (verifiable via `gh repo view kocaemre/zama-skills --json visibility`)."
    - "marketplace.json is reachable at the public raw URL `https://raw.githubusercontent.com/kocaemre/zama-skills/main/.claude-plugin/marketplace.json`."
    - "docs/marketplace-test.md documents the exact steps the user runs in Claude Code to verify the install flow."
  artifacts:
    - path: docs/marketplace-test.md
      provides: user-facing verification checklist for marketplace install
  key_links:
    - from: README.md
      to: ".claude-plugin/marketplace.json"
      via: "/plugin marketplace add github.com/kocaemre/zama-skills" command
      pattern: "marketplace add github.com/kocaemre/zama-skills"
---

<objective>
Flip repo to public + verify marketplace install path. Implements DIST-05.

Purpose: The README install snippet relies on the repo being public. Today it's private. After flipping, the user must run the `/plugin marketplace add` flow inside Claude Code (Claude can't run slash commands inside its own session) to confirm the round-trip works.
Output: public repo + user-facing verification doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-distribution-submission/06-CONTEXT.md
@.claude-plugin/marketplace.json
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make repo public + verify marketplace.json reachability</name>
  <files>docs/marketplace-test.md</files>
  <action>
    1. Confirm current visibility: `gh repo view kocaemre/zama-skills --json visibility -q .visibility` — expect `PRIVATE`.
    2. Flip to public (locked decision per CONTEXT.md): `gh repo edit kocaemre/zama-skills --visibility public --accept-visibility-change-consequences`.
    3. Verify: `gh repo view kocaemre/zama-skills --json visibility -q .visibility` — must print `PUBLIC`.
    4. Verify marketplace.json is reachable anonymously: `curl -fsSL https://raw.githubusercontent.com/kocaemre/zama-skills/main/.claude-plugin/marketplace.json | jq -e '.name == "zama-skills"'` — must exit 0.
    5. Write docs/marketplace-test.md (~40 lines) with sections:
       - **Pre-flight** — user has Claude Code installed; repo is public (just flipped).
       - **Steps** (numbered):
         1. Open Claude Code in any directory.
         2. Run `/plugin marketplace add github.com/kocaemre/zama-skills`.
            Expected: "Marketplace added: zama-skills" message.
         3. Run `/plugin install zama-skills@zama-skills`.
            Expected: install confirmation; 5 skills registered.
         4. Run `/plugin list` (or whatever the inspection command is). Confirm `zama-skills` plugin shows 5 skills.
         5. Type `/zama-skills:` and verify autocomplete suggests `init`, `contract`, `test`, `deploy`, `frontend`.
         6. Optional: run `/zama-skills:init --help` to confirm the skill loads without errors.
       - **Failure modes** — if step 2 fails with "marketplace.json not found", check repo visibility; if step 3 fails with schema error, check `.claude-plugin/plugin.json` exists in `plugins/zama-skills/` (Phase 1 artifact).
       - **Report back** — paste a screenshot or terminal log of step 5 (autocomplete listing) into the bounty submission notes.
    Commit: `chore(06-05): make repo public + document marketplace install verification`.
  </action>
  <verify>
    <automated>gh repo view kocaemre/zama-skills --json visibility -q .visibility | grep -q PUBLIC && curl -fsSL https://raw.githubusercontent.com/kocaemre/zama-skills/main/.claude-plugin/marketplace.json | jq -e '.name == "zama-skills"' && test -f docs/marketplace-test.md && grep -q '/plugin marketplace add github.com/kocaemre/zama-skills' docs/marketplace-test.md</automated>
  </verify>
  <done>Repo public; marketplace.json fetchable anonymously; verification doc written.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User runs marketplace install in Claude Code</name>
  <what-built>
    Claude flipped repo to public, confirmed marketplace.json is reachable at the raw URL, and wrote `docs/marketplace-test.md` with the verification steps.
  </what-built>
  <how-to-verify>
    Open `docs/marketplace-test.md` and execute steps 1–5 in your Claude Code session. Specifically:
    1. `/plugin marketplace add github.com/kocaemre/zama-skills`
    2. `/plugin install zama-skills@zama-skills`
    3. Type `/zama-skills:` and capture the autocomplete listing (screenshot or paste).
    4. Confirm 5 skills appear: init, contract, test, deploy, frontend.
    Report any error messages verbatim. If everything works, save the screenshot to `docs/marketplace-test-screenshot.png` (or paste a code-block log into the doc).
  </how-to-verify>
  <resume-signal>Type "marketplace verified" + paste log/screenshot path, or describe the failure.</resume-signal>
</task>

</tasks>

<verification>
- gh API confirms PUBLIC.
- curl + jq confirms marketplace.json shape.
- User confirms in-Claude install flow works.
</verification>

<success_criteria>
- Repo public.
- marketplace.json fetchable anonymously.
- User-validated install flow.
- DIST-05 satisfied.
</success_criteria>

<output>
Create `.planning/phases/06-distribution-submission/06-05-SUMMARY.md` after execution.
</output>
