---
plan: 06-02-generic-docs-generator
phase: 06
wave: 1
type: execute
autonomous: true
depends_on: []
requirements: [DIST-02]
files_modified:
  - scripts/generate-generic-docs.mjs
  - scripts/generate-generic-docs.test.mjs
  - generic/zama-init.md
  - generic/zama-contract.md
  - generic/zama-test.md
  - generic/zama-deploy.md
  - generic/zama-frontend.md
  - .github/workflows/ci.yml
task_count: 3
objective: Auto-generate `generic/<name>.md` from each SKILL.md so non-Claude AI agents (Cursor / Copilot / etc.) get an equivalent rehber, with CI drift gate.
must_haves:
  truths:
    - "Running `node scripts/generate-generic-docs.mjs` produces generic/{zama-init,zama-contract,zama-test,zama-deploy,zama-frontend}.md."
    - "Each generic doc contains: source SKILL.md SHA in frontmatter, stripped Claude-specific syntax (`allowed-tools`, `${CLAUDE_SKILL_DIR}`, `disable-model-invocation`), pointer back to canonical SKILL.md."
    - "CI fails when committed generic/*.md differs from fresh generator output (drift detection)."
    - "Generator has unit tests covering: SHA stamping, claude-syntax stripping, plugin-source-relative path resolution."
  artifacts:
    - path: scripts/generate-generic-docs.mjs
      provides: generator that reads plugins/zama-skills/skills/*/SKILL.md → writes generic/*.md
    - path: scripts/generate-generic-docs.test.mjs
      provides: vitest tests for generator
    - path: generic/zama-init.md
      provides: generic-agent rehber for /zama-init
    - path: generic/zama-contract.md
      provides: generic-agent rehber for /zama-contract
    - path: generic/zama-test.md
      provides: generic-agent rehber for /zama-test
    - path: generic/zama-deploy.md
      provides: generic-agent rehber for /zama-deploy
    - path: generic/zama-frontend.md
      provides: generic-agent rehber for /zama-frontend
    - path: .github/workflows/ci.yml
      provides: drift check job that re-runs generator and `git diff --exit-code generic/`
  key_links:
    - from: scripts/generate-generic-docs.mjs
      to: plugins/zama-skills/skills/*/SKILL.md
      via: fs.readFileSync per skill
      pattern: "skills/.*/SKILL.md"
    - from: .github/workflows/ci.yml
      to: scripts/generate-generic-docs.mjs
      via: npm script
      pattern: "generate-generic-docs"
---

<objective>
Auto-generate generic markdown rehberler from each SKILL.md so Cursor / Copilot / other AI agent users have a copy-pasteable equivalent. Implements DIST-02.

Purpose: Bounty submission claims "AI agent skills" — not "Claude-only skills." Without generic/*.md the value prop is half-truth. Generator enforces single source of truth (SKILL.md) and drift CI prevents silent rot.
Output: generator script + 5 generic docs + drift-detection CI job.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-distribution-submission/06-CONTEXT.md
@plugins/zama-skills/skills/init/SKILL.md
@scripts/example-smoke-diff.mjs
@scripts/build.ts
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for generator (RED)</name>
  <files>scripts/generate-generic-docs.test.mjs</files>
  <behavior>
    - Test 1: `generateGenericDoc({ skillPath })` returns a string containing the SKILL.md `name` and `description` from frontmatter.
    - Test 2: Output frontmatter includes a `source_sha` field equal to the git blob SHA of the source SKILL.md (computed via `git hash-object`).
    - Test 3: Output frontmatter includes `source: plugins/zama-skills/skills/<name>/SKILL.md` pointer.
    - Test 4: Output strips Claude-specific keys (`allowed-tools`, `disable-model-invocation`, `context: fork`) from the rendered frontmatter — they appear instead in a "Claude-specific notes" appendix section.
    - Test 5: Output replaces `${CLAUDE_SKILL_DIR}` substring in body with `<plugin-skill-dir>` (generic placeholder) and adds a footnote explaining the substitution.
    - Test 6: `generateAll()` writes 5 files under `generic/` matching `zama-{init,contract,test,deploy,frontend}.md`.
    - Test 7: Re-running `generateAll()` produces byte-identical output (deterministic — required for CI drift).
    Use vitest, import generator from `./generate-generic-docs.mjs`. Mark tests pending — they MUST fail until Task 2 lands.
  </behavior>
  <action>
    Write scripts/generate-generic-docs.test.mjs implementing the 7 tests above. Use `import { generateGenericDoc, generateAll } from './generate-generic-docs.mjs'`. For Test 2, shell out to `git hash-object <skill-path>` via `execSync` and compare. For Test 7, call `generateAll()` twice into `mktempSync`-style temp dirs and diff results. Run `npx vitest run scripts/generate-generic-docs.test.mjs` — must FAIL because generate-generic-docs.mjs does not exist yet.
  </action>
  <verify>
    <automated>npx vitest run scripts/generate-generic-docs.test.mjs 2>&1 | grep -qE '(failed|Cannot find module|ENOENT)'</automated>
  </verify>
  <done>Test file committed; vitest run fails (RED); commit message: `test(06-02): RED tests for generic docs generator`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement generator + emit 5 generic docs (GREEN)</name>
  <files>scripts/generate-generic-docs.mjs, generic/zama-init.md, generic/zama-contract.md, generic/zama-test.md, generic/zama-deploy.md, generic/zama-frontend.md</files>
  <behavior>
    - All 7 tests from Task 1 pass.
    - Generator exports `generateGenericDoc({ skillPath }): string` and `generateAll({ outDir }): Promise<string[]>`.
    - Output frontmatter shape:
      ```yaml
      ---
      name: <slug>
      description: <copied from SKILL.md>
      when_to_use: <copied>
      source: plugins/zama-skills/skills/<name>/SKILL.md
      source_sha: <git hash-object output>
      generator: scripts/generate-generic-docs.mjs
      ---
      ```
    - Body: copied SKILL.md body with Claude-specific tokens transformed; appendix section "## Claude-specific notes" lists the original `allowed-tools` / `disable-model-invocation` / `context: fork` values verbatim so power users know what was filtered.
    - Determinism: no timestamps, no PWD-dependent paths, frontmatter keys in fixed order.
  </behavior>
  <action>
    1. Implement scripts/generate-generic-docs.mjs. Reuse YAML frontmatter parsing pattern from scripts/build.ts (regex-based — keep zero external deps beyond Node built-ins + `node:child_process` for git hash-object).
    2. Run `node scripts/generate-generic-docs.mjs` to materialize the 5 generic/*.md files.
    3. Run `npx vitest run scripts/generate-generic-docs.test.mjs` — all 7 tests pass.
    4. Run generator a second time and `git diff --exit-code generic/` — no diff (proves determinism).
    Commit message: `feat(06-02): implement generic docs generator + materialize 5 docs`.
  </action>
  <verify>
    <automated>npx vitest run scripts/generate-generic-docs.test.mjs && node scripts/generate-generic-docs.mjs && git diff --exit-code generic/ && ls generic/zama-init.md generic/zama-contract.md generic/zama-test.md generic/zama-deploy.md generic/zama-frontend.md</automated>
  </verify>
  <done>Generator + 5 generic docs committed; tests green; second run produces no diff.</done>
</task>

<task type="auto">
  <name>Task 3: Add CI drift check + npm script</name>
  <files>.github/workflows/ci.yml, package.json</files>
  <action>
    1. Add npm script to package.json: `"generic:check": "node scripts/generate-generic-docs.mjs && git diff --exit-code generic/"`.
    2. Add a step to `.github/workflows/ci.yml` (in the existing `validate` job, after the "Run tests" step):
       ```yaml
       - name: Generic docs drift check
         run: npm run generic:check
       ```
       Job timeout already 10 min — sufficient.
    3. Verify locally: `npm run generic:check` exits 0.
    4. Sanity test the gate: temporarily edit a generic/*.md file, re-run, confirm exit 1, then revert.
    Commit message: `ci(06-02): drift gate for generic docs`.
  </action>
  <verify>
    <automated>npm run generic:check && grep -q 'generic:check' .github/workflows/ci.yml && grep -q 'generic:check' package.json</automated>
  </verify>
  <done>CI drift gate wired; npm script available; manual sanity test of gate confirmed in commit message.</done>
</task>

</tasks>

<verification>
- `npm run generic:check` is green.
- Editing any SKILL.md without re-running the generator must fail CI on the next push.
</verification>

<success_criteria>
- 5 generic/*.md docs exist + are byte-identical to fresh generator output.
- CI gate prevents silent drift.
- DIST-02 satisfied.
</success_criteria>

<output>
Create `.planning/phases/06-distribution-submission/06-02-SUMMARY.md` after execution.
</output>
