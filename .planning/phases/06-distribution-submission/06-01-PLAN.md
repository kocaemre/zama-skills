---
plan: 06-01-readme-hero
phase: 06
wave: 1
type: execute
autonomous: true
depends_on: []
requirements: [DIST-01]
files_modified:
  - README.md
  - docs/demo-gif-capture.md
task_count: 2
objective: Rewrite root README hero so a judge sees install one-liner, 5-skill table, live URLs, and demo placeholder above the fold within 30 seconds.
must_haves:
  truths:
    - "First viewport contains: 30s value prop, install one-liner, 5-row skills table, live Sepolia + Vercel URLs, demo GIF placeholder."
    - "Demo GIF capture instructions exist in docs/demo-gif-capture.md so user can record and replace placeholder later."
    - "All `<owner>` placeholders replaced with `kocaemre`."
  artifacts:
    - path: README.md
      provides: hero section with install snippet + skills table + live URLs
      contains: "/plugin marketplace add github.com/kocaemre/zama-skills"
    - path: docs/demo-gif-capture.md
      provides: capture instructions for the 90-second demo GIF
  key_links:
    - from: README.md
      to: examples/confidential-token/README.md
      via: relative link
      pattern: "examples/confidential-token"
---

<objective>
Polish root README so it sells the differentiator in 30 seconds. Implements DIST-01.

Purpose: Bounty judges land on github.com/kocaemre/zama-skills and decide in <30s whether this submission is worth a deep look. The hero must answer "what / how do I install / can I see it live / what skills ship" without scrolling.
Output: rewritten README.md + demo GIF capture instructions doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@README.md
@examples/confidential-token/README.md
@plugins/zama-skills/.claude-plugin/marketplace.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README hero + replace owner placeholders</name>
  <files>README.md</files>
  <action>
    Edit README.md in place. Required changes:
    1. Replace every `<owner>` with `kocaemre` (homepage URL, install snippet, package.json reference if any). grep -c '<owner>' README.md must return 0 after.
    2. Hero block (above first `## Install`): keep the existing one-line value prop and "Built for" + "Differentiator" lines.
    3. Replace the `<!-- TODO Phase 6: ... -->` badge placeholders with a real badge row: CI status (github.com/kocaemre/zama-skills/actions), npm version (`npm zama-skills` — placeholder shield until DIST-04 publishes), Sepolia Verified (already-present link), Vercel Live (already-present link).
    4. Install section: keep both Claude Code plugin path (`/plugin marketplace add github.com/kocaemre/zama-skills` — drop https:// per Claude marketplace docs convention) and `npx zama-skills install` fallback.
    5. Demo GIF: replace the `examples/confidential-token/docs/demo.gif` placeholder line with a single `<!-- @sync:demo-gif -->` marker and a link to `docs/demo-gif-capture.md` so user can drop in the recorded GIF later. Do NOT delete the placeholder image reference; rewrite it as `![demo placeholder — see docs/demo-gif-capture.md](examples/confidential-token/docs/demo.gif)`.
    6. Skills table: keep current 5-row table, verify each command name matches `plugins/zama-skills/skills/<name>/SKILL.md` `name:` field. Update the "When it runs" column to use observable trigger phrases pulled from each SKILL.md `when_to_use`.
    7. Drop the `<!-- TODO Phase 6: expand "Demo" section ... -->` TODOs since this plan addresses them.
    8. Add a "Links" section near the bottom: GitHub repo, npm package URL (`https://www.npmjs.com/package/zama-skills`), example dApp folder, ROADMAP, LICENSE, THIRD_PARTY_LICENSES (forward reference — created in 06-03).
    Preserve the existing Compatibility, How it works, CI/quality gates, Roadmap, License, Acknowledgements sections — only the hero (top of file through Install + Try-it-live + skills table) is rewritten.
  </action>
  <verify>
    <automated>grep -c '<owner>' README.md | grep -q '^0$' && grep -q 'kocaemre/zama-skills' README.md && grep -q '/plugin marketplace add github.com/kocaemre/zama-skills' README.md && grep -q 'docs/demo-gif-capture.md' README.md && grep -q '@sync:demo-gif' README.md</automated>
  </verify>
  <done>README hero shows install + table + live URLs above the fold; no `<owner>` placeholders remain; demo GIF marker + capture-doc link present.</done>
</task>

<task type="auto">
  <name>Task 2: Write demo GIF capture instructions</name>
  <files>docs/demo-gif-capture.md</files>
  <action>
    Create new file docs/demo-gif-capture.md. Required sections:
    1. **Goal** — A 60-90 second silent GIF (≤8 MB, ≤30 fps, 1280×720 max) showing: (a) `/plugin marketplace add github.com/kocaemre/zama-skills` in Claude Code, (b) `/plugin install zama-skills@zama-skills`, (c) `/zama-skills:init token` running, (d) `pnpm hardhat compile` going green, (e) opening the live Vercel URL and decrypting a balance.
    2. **Recommended tools** — macOS: `Cleanshot X` or `Kap` (gif export); Linux: `peek`; cross-platform: `terminalizer` for terminal-only segments.
    3. **Capture script** — explicit step list with timing notes (e.g., "0:00–0:15 Claude Code install commands; 0:15–0:45 init scaffold; 0:45–1:00 compile; 1:00–1:30 frontend decrypt").
    4. **Output path** — drop final file at `examples/confidential-token/docs/demo.gif`. Update README's `<!-- @sync:demo-gif -->` marker to point to the new file (no manual edit needed if the path stays identical).
    5. **Optimization** — `gifsicle -O3 --lossy=80 demo.gif -o demo.gif` to keep <8 MB (GitHub README inline cap).
    6. **Alternative** — record an MP4 instead and host as YouTube unlisted; replace the README image with a thumbnail that links to the video.
    Length target: ~60 lines, scannable.
  </action>
  <verify>
    <automated>test -f docs/demo-gif-capture.md && grep -q 'gifsicle' docs/demo-gif-capture.md && grep -q 'examples/confidential-token/docs/demo.gif' docs/demo-gif-capture.md</automated>
  </verify>
  <done>Capture instructions complete enough that user can record + drop GIF in correct path without further questions.</done>
</task>

</tasks>

<verification>
- README first viewport readable on github.com — manual check after merge.
- All cross-links in README resolve (no dead `<owner>` strings).
</verification>

<success_criteria>
- README.md hero rewritten, no placeholders.
- docs/demo-gif-capture.md exists with concrete capture procedure.
- DIST-01 satisfied (demo GIF marked as TODO with capture path documented — locked decision per CONTEXT.md).
</success_criteria>

<output>
Create `.planning/phases/06-distribution-submission/06-01-SUMMARY.md` after execution.
</output>
