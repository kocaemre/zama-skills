---
plan: 06-04-npm-package
phase: 06
wave: 2
type: execute
autonomous: false
depends_on: [06-02-generic-docs-generator]
requirements: [PLUGIN-05, DIST-04]
files_modified:
  - package.json
  - bin/zama-skills.mjs
  - src/cli/install.ts
  - src/cli/index.ts
  - src/cli/install.test.ts
  - LICENSE
task_count: 4
objective: Polish package.json + implement real `zama-skills install` CLI (commander + prompts + fs-extra) that copies skill bundles to `~/.claude/skills/` or project-scoped `.claude/skills/`, then hand off to user for `npm publish`.
must_haves:
  truths:
    - "Running `npm pack` produces a tarball containing: bin/, src/cli/, scripts/generate-generic-docs.mjs, plugins/, .claude-plugin/, README.md, LICENSE, THIRD_PARTY_LICENSES.md."
    - "Running `node bin/zama-skills.mjs install --scope personal` copies plugins/zama-skills/skills/* to `~/.claude/skills/zama-skills/` (or `--scope project` → `./.claude/skills/zama-skills/`) and prints next-steps."
    - "package.json `homepage`, `repository`, `bugs` URLs point to github.com/kocaemre/zama-skills (no `<owner>` placeholder)."
    - "package.json `version` bumped from 0.0.0-dev to 0.1.0 (first published version)."
    - "LICENSE file exists at repo root (MIT, copyright 2026 Emre Koca)."
    - "User can run `npm publish` interactively (npm login already done by user)."
  artifacts:
    - path: package.json
      provides: publishable npm manifest
      contains: '"version": "0.1.0"'
    - path: bin/zama-skills.mjs
      provides: CLI entry point (already exists; may need minor tweak)
    - path: src/cli/install.ts
      provides: install command implementation (copies skill bundles)
    - path: src/cli/index.ts
      provides: commander wiring
    - path: src/cli/install.test.ts
      provides: vitest tests for copy behavior
    - path: LICENSE
      provides: MIT license file required by npm
  key_links:
    - from: src/cli/install.ts
      to: plugins/zama-skills/skills
      via: fs-extra.copy
      pattern: "fs.copy|copySync|cp\\("
---

<objective>
Make the package publishable + ship a working `zama-skills install` CLI. Implements PLUGIN-05 + DIST-04 (Claude prep; user runs `npm publish`).

Purpose: Bounty README claims `npx zama-skills install` works. Today the bin script just prints recommended `/plugin` commands — must actually copy files. Also need clean package.json + LICENSE before npm will accept publish.
Output: working CLI + publishable package + handoff checkpoint for user to run `npm publish`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-distribution-submission/06-CONTEXT.md
@package.json
@bin/zama-skills.mjs
@src/cli/index.ts
@plugins/zama-skills/.claude-plugin/marketplace.json

<interfaces>
Existing src/cli/index.ts exports `program` (commander Command) and registers `install` command as a stub.
Existing bin/zama-skills.mjs spawns tsx on src/cli/index.ts.
plugins/zama-skills/skills/ contains: contract/, deploy/, frontend/, init/, test/ (each with SKILL.md + assets/ + scripts/).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for install command (RED)</name>
  <files>src/cli/install.test.ts</files>
  <behavior>
    - Test 1: `installSkills({ scope: 'personal', targetRoot, sourceRoot })` copies all 5 skill folders into `<targetRoot>/.claude/skills/zama-skills/<skill-name>/SKILL.md` (use a tmp dir as targetRoot, point sourceRoot at the repo's plugins/zama-skills/skills/).
    - Test 2: `--scope project` writes to `<cwd>/.claude/skills/zama-skills/` instead of `~/.claude/skills/`.
    - Test 3: Existing files at the destination prompt-overwrite path: when `force: true` is passed, files overwrite without prompt.
    - Test 4: Returns `{ written: number, scope, target }` summary.
    - Test 5: Throws if sourceRoot does not contain at least one `*/SKILL.md`.
    Use vitest + fs-extra + os.tmpdir(). Import from `./install.ts`.
  </behavior>
  <action>
    Create src/cli/install.test.ts implementing the 5 tests above. `import { installSkills } from './install.js'` (Note: `.js` extension for ESM TS compat; tsx resolves). Run `npx vitest run src/cli/install.test.ts` — must FAIL because install.ts not yet implemented.
    Commit: `test(06-04): RED tests for install CLI`.
  </action>
  <verify>
    <automated>npx vitest run src/cli/install.test.ts 2>&1 | grep -qE '(failed|Cannot find module|ENOENT)'</automated>
  </verify>
  <done>Test file committed; vitest run fails (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement install CLI + wire commander (GREEN)</name>
  <files>src/cli/install.ts, src/cli/index.ts</files>
  <behavior>
    - All 5 tests from Task 1 pass.
    - `installSkills({ scope, targetRoot, sourceRoot, force })` uses `fs-extra.copy` with `overwrite: force, errorOnExist: !force`.
    - `index.ts` `install` command:
      - Default `--scope personal` → targetRoot = `os.homedir()`.
      - `--scope project` → targetRoot = `process.cwd()`.
      - `--force` flag short-circuits the prompts overwrite confirmation.
      - Without `--force`, if any destination file exists, ask via `prompts({ type: 'confirm', name: 'ok', message: 'Overwrite existing skills?' })`.
      - Source root resolves to the package's bundled plugins/zama-skills/skills/ — use `path.resolve(fileURLToPath(import.meta.url), '../../../plugins/zama-skills/skills')`.
      - Print colored next-steps after success: "Run `/zama-skills:init` in Claude Code to scaffold your first dApp."
  </behavior>
  <action>
    1. Create src/cli/install.ts with `installSkills` implementation per behavior block.
    2. Replace the stub `install` command body in src/cli/index.ts with a real action handler that calls `installSkills` and handles the prompts flow.
    3. Run `npx vitest run src/cli/install.test.ts` — all green.
    4. Manual smoke: `node bin/zama-skills.mjs install --scope project --force` in a tmp dir, verify `.claude/skills/zama-skills/init/SKILL.md` exists.
    Commit: `feat(06-04): real install CLI copies skill bundles`.
  </action>
  <verify>
    <automated>npx vitest run src/cli/install.test.ts && (TMP=$(mktemp -d) && cd "$TMP" && node "$OLDPWD/bin/zama-skills.mjs" install --scope project --force && test -f "$TMP/.claude/skills/zama-skills/init/SKILL.md")</automated>
  </verify>
  <done>Install CLI works end-to-end; tests green; manual smoke passes.</done>
</task>

<task type="auto">
  <name>Task 3: Polish package.json + add LICENSE for npm publish</name>
  <files>package.json, LICENSE</files>
  <action>
    1. Update package.json:
       - `version`: `"0.1.0"` (first published).
       - `homepage`, `repository.url`, `bugs.url`: replace `<owner>` with `kocaemre`.
       - Add `"bugs": { "url": "https://github.com/kocaemre/zama-skills/issues" }`.
       - `files`: ensure includes `bin`, `src`, `scripts/generate-generic-docs.mjs`, `plugins`, `.claude-plugin`, `README.md`, `LICENSE`, `THIRD_PARTY_LICENSES.md`, `generic`. Drop `scripts` glob if it pulls test files; replace with explicit list.
       - Add `"prepublishOnly": "npm run validate && npm test && npm run generic:check"` to scripts.
       - Add `"description"` if not already accurate (current is fine).
       - Verify `bin.zama-skills` points to `./bin/zama-skills.mjs` (already does).
    2. Create LICENSE file at repo root with standard MIT text, "Copyright (c) 2026 Emre Koca".
    3. Run `npm pack --dry-run` and verify the file list includes: bin/, src/cli/, plugins/zama-skills/skills/, .claude-plugin/marketplace.json, generic/*.md, README.md, LICENSE, THIRD_PARTY_LICENSES.md.
    Commit: `chore(06-04): polish package.json + add LICENSE for npm publish`.
  </action>
  <verify>
    <automated>test -f LICENSE && grep -q 'MIT' LICENSE && grep -q 'kocaemre/zama-skills' package.json && ! grep -q '<owner>' package.json && grep -q '"version": "0.1.0"' package.json && grep -q 'prepublishOnly' package.json && npm pack --dry-run 2>&1 | grep -q 'plugins/zama-skills/skills/init/SKILL.md'</automated>
  </verify>
  <done>package.json publishable; LICENSE exists; `npm pack --dry-run` shows correct contents; `prepublishOnly` gate wired.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: User runs `npm publish`</name>
  <what-built>
    Claude has prepared the package: bumped version to 0.1.0, added LICENSE, wired prepublishOnly gate (validate + test + generic:check), implemented real install CLI, verified `npm pack --dry-run` contents.
  </what-built>
  <how-to-verify>
    1. From the repo root, ensure you're logged in: `npm whoami` (should print your npm username; if not, `npm login`).
    2. Verify the name is available: `npm view zama-skills version` — if it returns a version, the name is taken; if it 404s ("npm error 404 Not Found"), name is free.
    3. Dry-run one more time to inspect the tarball: `npm publish --dry-run`.
    4. Publish: `npm publish --access public` (the `--access public` flag is required for first publish on a scoped name; `zama-skills` is unscoped so flag is harmless).
    5. Verify it landed: `npm view zama-skills version` should print `0.1.0`.
    6. Smoke-test from outside the repo: `cd $(mktemp -d) && npx zama-skills@0.1.0 install --scope project --force`. Confirm `.claude/skills/zama-skills/init/SKILL.md` written.
    7. Report back the published version + tarball URL (`npm view zama-skills dist.tarball`).
  </how-to-verify>
  <resume-signal>Type "published" with the version + tarball URL, or describe any error from `npm publish`.</resume-signal>
</task>

</tasks>

<verification>
- `npm view zama-skills version` returns `0.1.0` after user step.
- `npx zama-skills@0.1.0 install --scope project --force` works in fresh dir.
- DIST-04 + PLUGIN-05 satisfied.
</verification>

<success_criteria>
- Real install CLI committed + tested.
- package.json + LICENSE ready for publish.
- User has published 0.1.0 to npm.
</success_criteria>

<output>
Create `.planning/phases/06-distribution-submission/06-04-SUMMARY.md` after execution.
</output>
