---
plan: 06-06-clean-vm-and-submission
phase: 06
wave: 3
type: execute
autonomous: false
depends_on: [06-04-npm-package, 06-05-public-and-marketplace-test]
requirements: [DIST-06, DIST-07]
files_modified:
  - scripts/clean-vm-test.sh
  - docs/SUBMISSION-CHECKLIST.md
task_count: 3
objective: Provide a clean-VM smoke script + submission checklist; user runs the smoke + submits the bounty form.
must_haves:
  truths:
    - "scripts/clean-vm-test.sh simulates a clean-VM install end-to-end (mktemp dir → npx zama-skills install → verify SKILL.md files copied) and exits 0 on success."
    - "docs/SUBMISSION-CHECKLIST.md lists every item the user must verify before clicking Submit on the bounty form, plus the GitHub repo URL + npm package URL pre-filled."
    - "User has executed the smoke script + submitted the bounty form."
  artifacts:
    - path: scripts/clean-vm-test.sh
      provides: bash smoke harness for fresh-environment install
    - path: docs/SUBMISSION-CHECKLIST.md
      provides: pre-submission checklist + final review steps
  key_links:
    - from: scripts/clean-vm-test.sh
      to: npm registry (npx zama-skills@latest)
      via: shell call
      pattern: "npx.*zama-skills"
---

<objective>
Smoke-test the published package on a clean filesystem + ship the submission. Implements DIST-06 + DIST-07.

Purpose: A judge will install from npm or marketplace into a fresh environment. The clean-VM script simulates that worst case. Submission checklist guarantees nothing is forgotten when the user fills the bounty form.
Output: smoke script + checklist + actual submission.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-distribution-submission/06-CONTEXT.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write clean-VM smoke script</name>
  <files>scripts/clean-vm-test.sh</files>
  <action>
    Create scripts/clean-vm-test.sh (bash, `#!/usr/bin/env bash`, `set -euo pipefail`). Behavior:
    1. Print banner: "zama-skills clean-VM smoke test".
    2. Create tmp dir: `WORK=$(mktemp -d -t zama-skills-smoke-XXXXXX)` and `trap "rm -rf $WORK" EXIT`.
    3. `cd "$WORK"`.
    4. Run `npx --yes zama-skills@latest install --scope project --force`.
    5. Assertions (each on failure: print red error, exit 1):
       - `[ -d .claude/skills/zama-skills ]`
       - `[ -f .claude/skills/zama-skills/init/SKILL.md ]`
       - `[ -f .claude/skills/zama-skills/contract/SKILL.md ]`
       - `[ -f .claude/skills/zama-skills/test/SKILL.md ]`
       - `[ -f .claude/skills/zama-skills/deploy/SKILL.md ]`
       - `[ -f .claude/skills/zama-skills/frontend/SKILL.md ]`
       - `grep -q 'disable-model-invocation: true' .claude/skills/zama-skills/deploy/SKILL.md` (PLUGIN-03 spot check survives the round-trip)
    6. Optional second phase (gated by `--with-marketplace` flag): print marketplace-add instructions for user to run in Claude Code (Claude Code is interactive — can't be invoked from bash).
    7. On success print green "✓ clean-VM smoke passed" + path to the temp dir for inspection (note: trap deletes it on exit; use `--keep` flag to preserve).
    Make script executable: `chmod +x scripts/clean-vm-test.sh`.
    Commit: `feat(06-06): clean-VM smoke harness`.
  </action>
  <verify>
    <automated>test -x scripts/clean-vm-test.sh && bash -n scripts/clean-vm-test.sh && grep -q 'npx --yes zama-skills@latest install' scripts/clean-vm-test.sh && grep -q 'disable-model-invocation: true' scripts/clean-vm-test.sh</automated>
  </verify>
  <done>Script written, executable, syntactically valid (`bash -n` clean). Note: cannot actually run until 06-04 user-step publishes to npm.</done>
</task>

<task type="auto">
  <name>Task 2: Write submission checklist</name>
  <files>docs/SUBMISSION-CHECKLIST.md</files>
  <action>
    Create docs/SUBMISSION-CHECKLIST.md (~80 lines) with sections:

    **# zama-skills — Bounty Submission Checklist**

    Submission target: 2026-05-09 (≥24h before 2026-05-10 23:59 AOE deadline).

    **## Pre-submission verification**
    - [ ] All 41 v1 requirements complete (cross-check `.planning/REQUIREMENTS.md` traceability table).
    - [ ] CI green on `main` (https://github.com/kocaemre/zama-skills/actions).
    - [ ] `npm view zama-skills version` returns 0.1.0 (or current published version).
    - [ ] `gh repo view kocaemre/zama-skills --json visibility` returns `PUBLIC`.
    - [ ] `bash scripts/clean-vm-test.sh` exits 0 (Task 1 of this plan).
    - [ ] In Claude Code: `/plugin marketplace add github.com/kocaemre/zama-skills` + `/plugin install zama-skills@zama-skills` works (06-05 verified).
    - [ ] Live Sepolia contract loads on Etherscan: https://sepolia.etherscan.io/address/0x04Bd105DE7a5D3297c3747cef90ac8b760136896#code
    - [ ] Live Vercel frontend loads: https://zama-skills.vercel.app and decryption round-trips.
    - [ ] README hero readable above the fold on github.com.
    - [ ] THIRD_PARTY_LICENSES.md present and complete.
    - [ ] generic/*.md present (5 files, all green from CI drift check).
    - [ ] Demo GIF: either committed at `examples/confidential-token/docs/demo.gif` or replaced with a "demo capture pending" note linking to `docs/demo-gif-capture.md`.

    **## Submission form fields**
    Pre-fill the bounty form with:
    - **GitHub repo URL:** `https://github.com/kocaemre/zama-skills`
    - **npm package URL:** `https://www.npmjs.com/package/zama-skills`
    - **Live demo URL:** `https://zama-skills.vercel.app`
    - **Live contract:** `https://sepolia.etherscan.io/address/0x04Bd105DE7a5D3297c3747cef90ac8b760136896#code`
    - **Short pitch (≤280 chars):** "5 Claude Code skills + npm package that take a developer from empty dir to deployed confidential dApp on Sepolia in 30 min. Every line verified live against Zama docs via context7 — zero hallucinated APIs. Live demo + verified contract included."
    - **Long description:** Copy from README hero + Why-this-exists section.
    - **Category:** Bounty Track / AI Agent Skills.

    **## Final actions**
    1. Tag the release: `git tag v0.1.0 && git push origin v0.1.0`.
    2. Create GitHub release: `gh release create v0.1.0 --title "v0.1.0 — Bounty submission" --notes-from-tag` (or paste hand-written notes).
    3. Submit the bounty form (URL: TBD — user has it).
    4. Post submission confirmation in the doc: append a `## Submission record` section with timestamp, submission ID, and any reviewer-facing notes.

    **## Post-submission**
    - Watch for jury questions on the GitHub repo issues / npm package page.
    - Do NOT push breaking changes to `main` until jury review is complete.

    Commit: `docs(06-06): submission checklist`.
  </action>
  <verify>
    <automated>test -f docs/SUBMISSION-CHECKLIST.md && grep -q 'kocaemre/zama-skills' docs/SUBMISSION-CHECKLIST.md && grep -q '2026-05-09' docs/SUBMISSION-CHECKLIST.md && grep -q 'clean-vm-test.sh' docs/SUBMISSION-CHECKLIST.md && [ "$(wc -l < docs/SUBMISSION-CHECKLIST.md)" -ge 50 ]</automated>
  </verify>
  <done>Checklist complete with all pre-flight items + submission form fields + final actions.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: User runs clean-VM smoke + submits bounty form</name>
  <what-built>
    Claude wrote scripts/clean-vm-test.sh (bash smoke harness against the published npm package) and docs/SUBMISSION-CHECKLIST.md (full pre-flight + form fields + final actions).
  </what-built>
  <how-to-verify>
    1. **Smoke test:** Run `bash scripts/clean-vm-test.sh`. Expect "✓ clean-VM smoke passed". If it fails, paste the error here.
    2. **Walk the checklist:** Open `docs/SUBMISSION-CHECKLIST.md` and tick every Pre-submission box. Stop and report any unchecked item.
    3. **Tag + release:**
       ```bash
       git tag v0.1.0
       git push origin v0.1.0
       gh release create v0.1.0 --title "v0.1.0 — Bounty submission" --notes "First public release. See README + DEPLOYED.md."
       ```
    4. **Submit the form:** Fill in the fields exactly as specified in `docs/SUBMISSION-CHECKLIST.md` "Submission form fields" section. Submit.
    5. **Record:** Append a `## Submission record` block at the bottom of `docs/SUBMISSION-CHECKLIST.md` with timestamp + submission ID/URL.
    6. Report back with submission confirmation (ID, screenshot, or success message).
  </how-to-verify>
  <resume-signal>Type "submitted" + submission ID/URL/timestamp, or describe any blocker.</resume-signal>
</task>

</tasks>

<verification>
- Clean-VM smoke green (after 06-04 publish completes).
- Submission checklist all-checked.
- Bounty form submitted ≥24h before deadline.
</verification>

<success_criteria>
- DIST-06 satisfied (clean-VM verified by user).
- DIST-07 satisfied (submission ≥24h early).
</success_criteria>

<output>
Create `.planning/phases/06-distribution-submission/06-06-SUMMARY.md` after execution.
</output>
