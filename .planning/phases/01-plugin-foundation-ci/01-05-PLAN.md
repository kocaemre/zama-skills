---
phase: 01-plugin-foundation-ci
plan_id: 05
title: README Skeleton — Sells Differentiator in 30 Seconds
type: execute
wave: 5
depends_on: [04]
files_modified:
  - README.md
autonomous: true
requirements: []
est_tasks: 1

must_haves:
  truths:
    - "First viewport (≈30 lines) makes the value prop, install command, and differentiator legible without scrolling"
    - "Both install paths documented: (1) `/plugin marketplace add` (primary) + (2) `npx zama-skills install` (fallback)"
    - "Skills table shows all 5 skills with their slash commands `/zama-skills:{init,contract,test,deploy,frontend}` and one-line purpose each"
    - "README.md exists at repo root, parses as valid markdown, no broken local links to files we haven't created yet"
    - "Phase 6 hook documented: which sections expand at submission (demo video, live dApp URL, judge-impressing badges)"
  artifacts:
    - path: "README.md"
      provides: "Repo-root README serving as judge's first impression + install instructions"
      min_lines: 80
      contains: "/plugin marketplace add"
  key_links:
    - from: "README.md install section"
      to: ".claude-plugin/marketplace.json + plugins/zama-skills/.claude-plugin/plugin.json"
      via: "documented `/plugin marketplace add` command targets the repo root where these files live"
      pattern: "/plugin marketplace add"
---

<objective>
Write a README that sells the project in the first 30 seconds (the judge's likely first surface). Phase 1 ships a SKELETON — the differentiator + install commands + skills table must be solid; the demo-video, live-dApp link, and badges-row are placeholders Phase 6 fills in. This is the foundation for DIST-01 (Phase 6).

Purpose: Make the repo legible at a glance; document the dual install path; preserve the "context7-aware skills (anti-hallucination)" differentiator front-and-center.
Output: `README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.claude-plugin/marketplace.json
@plugins/zama-skills/.claude-plugin/plugin.json
@package.json

# From PROJECT.md "Key Decisions":
# - Differentiator = "context7-aware skills" (anti-hallucination) — diğer
#   submission'larda yüksek ihtimalle yok; somut, gösterilebilir teknik avantaj
# - fhevm-react-template fork + customize (sıfırdan scaffold yerine) — Zama'nın
#   kendisi resmi olarak öneriyor
# - Submission ≥24 saat erken; deadline 2026-05-10 23:59 AOE

# From phase_info: "selling the differentiator in the first 30 seconds (judge will see this first)"
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md skeleton (judge-optimized layout)</name>
  <files>README.md</files>
  <action>
Write `README.md` at repo root. Layout optimized for the first viewport on GitHub (≈30 lines visible without scroll on a 1080p display).

```markdown
# zama-skills

> AI agent skills that take a developer from an empty directory to a deployed confidential dApp on Sepolia in 30 minutes — without writing a single FHE primitive by hand.

**Built for:** [Zama Developer Program — Mainnet Season 2 / Bounty Track](https://docs.zama.org/protocol/community/programs).

**Differentiator:** Every generated line is verified live against official Zama documentation via [context7](https://github.com/upstash/context7) MCP — `/zama-ai/fhevm` (1,772 snippets), `/zama-ai/fhevm-hardhat-template`, `/websites/openzeppelin_confidential-contracts`. **Zero hallucinated APIs.**

<!-- Phase 6 inserts: badges row (CI · npm · Sepolia demo dApp · 90-second video) -->
<!-- Phase 6 inserts: live demo screenshot or terminal cast -->

## Install

**Primary path — Claude Code plugin:**

```bash
# In Claude Code:
/plugin marketplace add https://github.com/<owner>/zama-skills
/plugin install zama-skills@zama-skills
```

**Fallback — npx (for non-Claude-Code agents or scripted setup):**

```bash
npx zama-skills install
```

> **Note:** Phase 1 (foundation) ships the plugin manifests and 5 skill skeletons. Skill bodies fill out across Phases 2–4. For the latest status see [`.planning/ROADMAP.md`](.planning/ROADMAP.md).

## What you get — 5 skills

| Slash command | When it runs | What it does |
|---------------|--------------|--------------|
| `/zama-skills:init` | "scaffold confidential dapp", empty dir | Forks `fhevm-react-template`, asks for use-case (token / voting / auction / custom), wires pinned versions, generates `.env.example` + MetaMask Sepolia deep-link |
| `/zama-skills:contract` | editing `.sol` in an fhevm project | Authors confidential contracts with correct `euint`/`ebool`/`eaddress`, ACL `FHE.allowThis`, OZ Confidential Contracts (ERC-7984); rejects `require(decrypt(...))` cleartext leaks |
| `/zama-skills:test` | "test fhevm", "mock encrypted input" | Generates mock + Sepolia integration tests with `@fhevm/hardhat-plugin`, decrypt assertions, HCU-budget warnings |
| `/zama-skills:deploy` | manual only — won't auto-trigger | Deploys to Sepolia + verifies on Etherscan + auto-registers Confidential Token Registry; pulls live addresses, never pins |
| `/zama-skills:frontend` | editing `src/` in an fhevm project | Wires `@zama-fhe/relayer-sdk`, `useDecrypted` hook with relayer UX states, ethers v6 + typechain, encrypted-input components |

`/zama-skills:deploy` has `disable-model-invocation: true` — Claude **will not** auto-deploy on its own. You must invoke it explicitly.

## Why this exists

Building a confidential dApp on Zama Protocol requires juggling: pinned `@fhevm/solidity` + `@openzeppelin/confidential-contracts` versions, ACL discipline (`FHE.allowThis` after every state write), HCU budget awareness (20M/tx, 5M depth), three different decryption paths (public / user / oracle), the Confidential Token Registry, and the relayer SDK on the frontend. Get any of it wrong — say, ship code that imports the deprecated `fhevmjs` package or forgets a single `FHE.allow` call — and your dApp silently fails on Sepolia.

`zama-skills` codifies the official patterns. Every skill, before generating code, queries context7 for the canonical Zama doc snippet that covers the user's request. **No "vibes-based FHE."**

## Compatibility

- **Network:** Sepolia testnet only (mainnet support deferred to v2 — needs auditing rigor).
- **Node.js:** `>=20`.
- **Solidity:** `^0.8.24+` (template default `0.8.27`).
- **Hardhat:** `^2.x` (Hardhat 3 is not supported by `@fhevm/hardhat-plugin` yet).
- **Ethers:** v6 only (v5 will mismatch typechain output).
- **Refuses to emit:** `fhevmjs` (deprecated 2025-07-10 → use `@zama-fhe/relayer-sdk`), root `fhevm` package (deprecated → use `@fhevm/solidity`), `ethers@5`, Hardhat 3.

## How it works

```
.claude-plugin/marketplace.json      ← catalog
plugins/zama-skills/
├── .claude-plugin/plugin.json        ← manifest
└── skills/
    ├── init/SKILL.md                 ← /zama-skills:init   (auto-invoke, context: fork)
    ├── contract/SKILL.md             ← /zama-skills:contract
    ├── test/SKILL.md                 ← /zama-skills:test
    ├── deploy/SKILL.md               ← /zama-skills:deploy (manual only)
    └── frontend/SKILL.md             ← /zama-skills:frontend
```

The plugin is a single Claude Code marketplace at the repo root. Skill folder names drop the `zama-` prefix (the plugin namespace already supplies it) so commands read `/zama-skills:init` not `/zama-skills:zama-init`.

## CI / quality gates

- `npm run validate` — zod schema check for `marketplace.json` + `plugin.json` + all 5 SKILL.md frontmatters (rejects: bad kebab-case, reserved marketplace names, `..` in source paths, missing `disable-model-invocation` on deploy, missing `context: fork` on init, missing `allowed-tools` on any skill, combined description+when_to_use over 1,536 chars).
- `npx tsc --noEmit` — strict TypeScript on the CLI.
- `npm test` — vitest.
- GitHub Actions runs all of the above on push and every PR.

## Roadmap

| Phase | Status | Goal |
|-------|--------|------|
| 1 — Plugin Foundation + CI | Active | Marketplace + manifests + 5 SKILL.md skeletons + CI gating |
| 2 — Shared Infrastructure | Pending | Pinned versions, deprecated-imports list, transclusion build engine |
| 3 — `/zama-skills:init` | Pending | Headline scaffolding skill — full flow |
| 4 — Remaining 4 skills | Pending | contract, test, deploy, frontend |
| 5 — Reference example dApp | Pending | Confidential token deployed on Sepolia + live frontend |
| 6 — Distribution / submission | Pending | npm publish, README polish, demo video, submit |

## License

[MIT](./LICENSE) © 2026 Emre Koca

## Acknowledgements

- [Zama](https://www.zama.ai) — fhEVM, FHE Solidity library, hardhat plugin, relayer SDK
- [OpenZeppelin](https://www.openzeppelin.com) — Confidential Contracts (ERC-7984)
- [Anthropic](https://www.anthropic.com) — Claude Code skills + plugins format
- [context7](https://github.com/upstash/context7) — live docs MCP that powers anti-hallucination
```

After writing:
- Verify file is at least 80 lines
- Verify both install commands are visible
- Verify skills table lists all 5 skills with the `/zama-skills:` namespace prefix
- Verify no link to a file that doesn't exist yet (other than `.planning/ROADMAP.md` which the user has confirmed exists)
  </action>
  <verify>
    <automated>test -f README.md && [ "$(wc -l < README.md)" -ge 80 ] && grep -q "/plugin marketplace add" README.md && grep -q "npx zama-skills install" README.md && for s in init contract test deploy frontend; do grep -q "/zama-skills:$s" README.md || exit 1; done && grep -q "context7" README.md && grep -q "disable-model-invocation: true" README.md && echo ok</automated>
  </verify>
  <done>
README.md exists at repo root, ≥80 lines; first viewport shows hero + differentiator (context7 anti-hallucination); both install paths documented; skills table lists all 5 with `/zama-skills:{slug}` slash commands; deploy's `disable-model-invocation: true` called out; phase roadmap visible; license + acknowledgements at the bottom.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public README → bounty judge / future contributors | First-impression surface; misleading claims = trust loss |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-README-1 | Repudiation / Misrepresentation | README claims features Phase 1 doesn't ship | mitigate | Explicit "Phase 1 ships skeletons; skill bodies fill out across Phases 2–4" note + Roadmap table with Status column |
| T-01-README-2 | Information Disclosure | Author email / personal info exposure | accept | `emrekoca2003@gmail.com` is the same email already used in `marketplace.json` + `plugin.json` + `package.json`; consistent public attribution per CLAUDE.md `userEmail` |
| T-01-README-3 | Tampering | `<owner>` placeholder shipped in install command misleads users | accept | Phase 6 submission task replaces `<owner>` after GitHub repo is created; explicitly tracked in PLAN-01 + PLAN-03 |
</threat_model>

<verification>
```bash
test -f README.md
[ "$(wc -l < README.md)" -ge 80 ]

# First-impression checks
grep -q "context7" README.md           # differentiator present
grep -q "anti-hallucination" README.md  # differentiator framed
grep -q "Sepolia" README.md             # network constraint clear
grep -q "MIT" README.md

# Both install paths
grep -q "/plugin marketplace add" README.md
grep -q "/plugin install zama-skills@zama-skills" README.md
grep -q "npx zama-skills install" README.md

# Skills table — all 5 with namespace
for s in init contract test deploy frontend; do
  grep -q "/zama-skills:$s" README.md || { echo "missing $s"; exit 1; }
done

# Deploy's manual-invocation contract surfaced
grep -q "disable-model-invocation: true" README.md
```
</verification>

<success_criteria>
- README.md exists with ≥80 lines
- Differentiator (context7 anti-hallucination) is in the first viewport
- Both install paths visible early
- All 5 skills documented with their slash commands
- Phase 1 scope honestly framed (skeletons, not full implementations)
- Roadmap table shows future phases
- License + acknowledgements present
- Foundation set for Phase 6 to expand into DIST-01 polish (demo video, live URL, badges)
</success_criteria>

<output>
Implementation only. Phase SUMMARY produced by checker after PLAN-05 completes.
</output>
