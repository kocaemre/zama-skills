---
phase: 01-plugin-foundation-ci
plan_id: 02
title: 5 SKILL.md Skeletons with Valid Frontmatter
type: execute
wave: 2
depends_on: [01]
files_modified:
  - plugins/zama-skills/skills/init/SKILL.md
  - plugins/zama-skills/skills/contract/SKILL.md
  - plugins/zama-skills/skills/test/SKILL.md
  - plugins/zama-skills/skills/deploy/SKILL.md
  - plugins/zama-skills/skills/frontend/SKILL.md
autonomous: true
requirements: [PLUGIN-02, PLUGIN-03, PLUGIN-04]
est_tasks: 2

must_haves:
  truths:
    - "5 SKILL.md files exist at plugins/zama-skills/skills/{init,contract,test,deploy,frontend}/SKILL.md"
    - "Folder names DROP the `zama-` prefix (per phase_info user-confirmed namespace decision); slash commands resolve as `/zama-skills:init`, `/zama-skills:contract`, etc."
    - "Every SKILL.md has YAML frontmatter with: `name`, `description`, `when_to_use`, `allowed-tools`"
    - "Combined `description` + `when_to_use` ≤ 1,536 chars in every skill (lead with key use case + trigger phrases)"
    - "ONLY `deploy/SKILL.md` has `disable-model-invocation: true`"
    - "ONLY `init/SKILL.md` has `context: fork`"
    - "All 5 skill `name` values are kebab-case, ≤64 chars (`init`, `contract`, `test`, `deploy`, `frontend`)"
  artifacts:
    - path: "plugins/zama-skills/skills/init/SKILL.md"
      provides: "Auto-invoke skill skeleton — scaffolds new fhEVM dApp; uses context: fork"
      contains: "name: init"
    - path: "plugins/zama-skills/skills/contract/SKILL.md"
      provides: "Auto-invoke skill skeleton — confidential contract authoring assistant"
      contains: "name: contract"
    - path: "plugins/zama-skills/skills/test/SKILL.md"
      provides: "Auto-invoke skill skeleton — FHE test pattern generator"
      contains: "name: test"
    - path: "plugins/zama-skills/skills/deploy/SKILL.md"
      provides: "MANUAL-ONLY skill skeleton — Sepolia deploy (disable-model-invocation: true)"
      contains: "disable-model-invocation: true"
    - path: "plugins/zama-skills/skills/frontend/SKILL.md"
      provides: "Auto-invoke skill skeleton — React + relayer-sdk integration"
      contains: "name: frontend"
  key_links:
    - from: "plugins/zama-skills/skills/{n}/SKILL.md frontmatter"
      to: "Claude Code runtime skill loader"
      via: "YAML frontmatter parsed at plugin load"
      pattern: "^---\\nname: (init|contract|test|deploy|frontend)\\n"
---

<objective>
Create 5 SKILL.md skeleton files with COMPLETE, SCHEMA-VALID frontmatter. Bodies are placeholder — Phase 3+ flesh them out. Phase 1 just needs Claude Code to load the plugin and recognize all 5 skills with the correct invocation properties.

Purpose: Satisfy PLUGIN-02 (5 skills), PLUGIN-03 (deploy manual-only), PLUGIN-04 (allowed-tools whitelist on every skill).
Output: 5 SKILL.md files at canonical paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-plugin-foundation-ci/01-CONTEXT.md
@.planning/phases/01-plugin-foundation-ci/01-RESEARCH.md
@plugins/zama-skills/.claude-plugin/plugin.json

# Frontmatter spec (HIGH confidence, RESEARCH.md):
# - `name`: kebab-case, ≤64 chars, lowercase letters/digits/hyphens only
# - `description` + `when_to_use` combined CAP = 1,536 chars (silently truncated past)
# - `allowed-tools`: space-separated tool whitelist (Bash patterns use parens)
# - `disable-model-invocation: true` → only user can invoke (NOT auto-trigger)
# - `context: fork` → runs in isolated subagent (good for noisy scaffolding)
# - `${CLAUDE_SKILL_DIR}` → substituted at runtime to skill's own directory

# NAMESPACE DECISION (user-confirmed in phase_info, resolves RESEARCH.md A5):
# Plugin name is `zama-skills`. Skill folders DROP the `zama-` prefix.
# On disk: skills/init/SKILL.md   →   slash command: /zama-skills:init
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the 4 auto-invoke SKILL.md skeletons (init, contract, test, frontend)</name>
  <files>
plugins/zama-skills/skills/init/SKILL.md,
plugins/zama-skills/skills/contract/SKILL.md,
plugins/zama-skills/skills/test/SKILL.md,
plugins/zama-skills/skills/frontend/SKILL.md
  </files>
  <action>
Create 4 SKILL.md files. Each has YAML frontmatter + a placeholder body section. Bodies stay minimal — Phase 3+ owns content; Phase 1 only needs schema-valid frontmatter so the plugin loads and skills register.

**FRONTMATTER RULES (apply to all 4):**
- `name` matches folder name exactly: `init`, `contract`, `test`, `frontend`
- `description` leads with a one-sentence "what + when" (≤200 chars) for auto-invoke matching
- `when_to_use` lists 3-5 trigger phrases the user might type
- Combined `description` + `when_to_use` length MUST be ≤ 1,536 chars (count after rendering — keep both terse)
- `allowed-tools` whitelists ONLY tools the skill actually needs (NEVER blanket `Bash`)
- ONLY `init` gets `context: fork`
- NONE of these 4 get `disable-model-invocation`

---

**File 1: `plugins/zama-skills/skills/init/SKILL.md`**

```markdown
---
name: init
description: Scaffold a new confidential dApp from the official fhevm-react-template, customized for the user's chosen use-case (token, voting, auction, custom). Use when the user wants to bootstrap a new Zama Protocol / fhEVM project from scratch.
when_to_use: Trigger phrases include "init zama project", "new fhevm dapp", "scaffold confidential token", "start zama", "bootstrap confidential dapp". Run when working in an empty or near-empty directory.
context: fork
allowed-tools: Bash(git *) Bash(npm *) Bash(npx *) Bash(mkdir *) Bash(cp *) Read Write Edit Glob Grep WebFetch
---

# /zama-skills:init — Skeleton (Phase 1)

Scaffold the official `fhevm-react-template` and customize for the user's confidential dApp use-case. Phase 3 fleshes out:
- Use-case branching (token / voting / auction / custom)
- Pinned-version injection from `shared/pinned-versions.json`
- `.env.example` generation (Sepolia RPC, mnemonic, Etherscan, relayer URL, registry)
- MetaMask Sepolia deep-link
- Closing summary

Resources will be referenced via `${CLAUDE_SKILL_DIR}/` once Phase 3 populates them.
```

---

**File 2: `plugins/zama-skills/skills/contract/SKILL.md`**

```markdown
---
name: contract
description: Author confidential Solidity contracts using @fhevm/solidity (euint, ebool, eaddress, ACL, FHE.allowThis). Use when the user wants to write or modify FHE-aware smart contracts, integrate OpenZeppelin Confidential Contracts (ERC-7984, governance), or pick a decryption path (public/user/oracle).
when_to_use: Trigger phrases include "write fhevm contract", "confidential token", "euint", "encrypted contract", "FHE.allow", "confidential ERC20". Run when editing .sol files in a fhevm project.
allowed-tools: Read Write Edit Glob Grep Bash(npm *) Bash(npx hardhat *) WebFetch
---

# /zama-skills:contract — Skeleton (Phase 1)

Confidential contract authoring assistant. Phase 4 implements:
- Correct `euint8/16/32/64`, `ebool`, `eaddress` typing; cleartext-leak rejection
- Mandatory `FHE.allowThis(handle)` after every state write
- OZ Confidential Contracts (ERC-7984) extend patterns
- Decryption path decision tree (public / user / oracle)
- HCU budget guidance (20M/tx, 5M depth)

All patterns sourced live from context7 (`/zama-ai/fhevm`, `/websites/openzeppelin_confidential-contracts`).
```

---

**File 3: `plugins/zama-skills/skills/test/SKILL.md`**

```markdown
---
name: test
description: Generate fhEVM tests using @fhevm/hardhat-plugin — encrypted-input mocks, decrypt assertions, Sepolia integration scaffolds. Use when the user wants to write or run tests for confidential contracts.
when_to_use: Trigger phrases include "test fhevm", "mock encrypted input", "decrypt assertion", "fhevm hardhat test", "sepolia integration test". Run when in a hardhat project with @fhevm dependencies.
allowed-tools: Read Write Edit Glob Grep Bash(npm test*) Bash(npx hardhat *) Bash(npm run *) WebFetch
---

# /zama-skills:test — Skeleton (Phase 1)

FHE test pattern generator. Phase 4 implements:
- Mock test pattern (`@fhevm/hardhat-plugin` encrypted-input mock + decrypt)
- Sepolia integration test scaffold (real deploy + real encrypted input)
- `FHE.allowThis` verification (post-call decrypt confirmation)
- HCU budget warning (mock won't catch overruns; Sepolia reverts)
```

---

**File 4: `plugins/zama-skills/skills/frontend/SKILL.md`**

```markdown
---
name: frontend
description: Integrate @zama-fhe/relayer-sdk into a React frontend with Wagmi/Viem or ethers v6 — encrypted input components, useDecrypted hook with relayer UX states, typechain wiring. Use when the user wants to build or extend the dApp UI.
when_to_use: Trigger phrases include "fhevm frontend", "relayer sdk", "useDecrypted", "encrypted input ui", "react fhe". Run when editing files under src/ or app/ in a fhevm project.
allowed-tools: Read Write Edit Glob Grep Bash(npm *) Bash(npx *) WebFetch
---

# /zama-skills:frontend — Skeleton (Phase 1)

Frontend integration assistant. Phase 4 implements:
- `@zama-fhe/relayer-sdk` `SepoliaConfig` init
- `useDecrypted(handle)` React hook with "awaiting relayer" state
- Encrypted-input component pattern
- ethers v6 + typechain wiring (rejects v5 with explicit error)

Will REFUSE to emit imports of deprecated `fhevmjs` or root `fhevm` packages (sourced from `shared/deprecated-imports.json` once Phase 2 ships it).
```

---

After writing, sanity-check combined frontmatter `description + when_to_use` length per file:

```bash
for skill in init contract test frontend; do
  node -e "
    const m=require('fs').readFileSync('plugins/zama-skills/skills/$skill/SKILL.md','utf8');
    const fm=m.match(/^---\\n([\\s\\S]*?)\\n---/)[1];
    const desc=(fm.match(/description:\\s*(.+)/m)||[])[1]||'';
    const wtu=(fm.match(/when_to_use:\\s*(.+)/m)||[])[1]||'';
    const total=(desc+wtu).length;
    if(total>1536){console.error('$skill: '+total+' >1536');process.exit(1);}
    console.log('$skill OK: '+total+' chars');
  "
done
```
  </action>
  <verify>
    <automated>for s in init contract test frontend; do test -f "plugins/zama-skills/skills/$s/SKILL.md" || exit 1; grep -q "^name: $s$" "plugins/zama-skills/skills/$s/SKILL.md" || exit 1; grep -q "^allowed-tools:" "plugins/zama-skills/skills/$s/SKILL.md" || exit 1; grep -q "disable-model-invocation:" "plugins/zama-skills/skills/$s/SKILL.md" && exit 1; done; grep -q "^context: fork$" plugins/zama-skills/skills/init/SKILL.md || exit 1; echo ok</automated>
  </verify>
  <done>
4 files exist, each has frontmatter with `name` matching folder, `description`, `when_to_use`, `allowed-tools`. Only `init` has `context: fork`. None of these 4 has `disable-model-invocation`. Combined description+when_to_use ≤1536 chars per file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the deploy SKILL.md (manual-only)</name>
  <files>plugins/zama-skills/skills/deploy/SKILL.md</files>
  <action>
Create the deploy skeleton — this is the ONLY skill with `disable-model-invocation: true` (per CONTEXT D-frontmatter and PLUGIN-03 requirement).

**File: `plugins/zama-skills/skills/deploy/SKILL.md`**

```markdown
---
name: deploy
description: Deploy compiled fhEVM contracts to Sepolia testnet, verify on Etherscan, and (if applicable) auto-register with the Confidential Token Registry. Use ONLY when the user explicitly asks to deploy.
when_to_use: User has compiled contracts and explicitly types "/zama-skills:deploy" or asks to deploy to Sepolia. Never auto-invoke — destructive on-chain action.
disable-model-invocation: true
allowed-tools: Read Write Bash(npx hardhat *) Bash(npm run *) Bash(node *) WebFetch
---

# /zama-skills:deploy — Skeleton (Phase 1)

Sepolia deploy + verify + registry registration. Phase 4 implements:
- Sepolia deploy script + Etherscan verify + ABI export
- Confidential Token Registry auto-registration (when token detected)
- Live `WebFetch` of Sepolia address list (`docs.zama.org/protocol/protocol-apps/addresses/testnet/sepolia`) — never pinned, Zama updates these
- `.env` validation with explicit error on missing vars (no half-deployed state)

`disable-model-invocation: true` is intentional — Claude must NEVER auto-deploy a contract because it "looks ready". User must explicitly invoke.
```

Critical:
- The `disable-model-invocation: true` line is REQUIRED (PLUGIN-03)
- Do NOT add `context: fork` (only `init` gets that)
- `allowed-tools` is narrow: deploy doesn't need git/mkdir/cp — just hardhat invocation, env reading, and webfetch for live addresses
  </action>
  <verify>
    <automated>test -f plugins/zama-skills/skills/deploy/SKILL.md && grep -q "^name: deploy$" plugins/zama-skills/skills/deploy/SKILL.md && grep -q "^disable-model-invocation: true$" plugins/zama-skills/skills/deploy/SKILL.md && grep -q "^allowed-tools:" plugins/zama-skills/skills/deploy/SKILL.md && ! grep -q "^context: fork" plugins/zama-skills/skills/deploy/SKILL.md && echo ok</automated>
  </verify>
  <done>
File exists, frontmatter has `name: deploy`, `disable-model-invocation: true`, `allowed-tools` set, NO `context: fork`. Combined description+when_to_use ≤1536 chars.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude model → user's filesystem/network | Skills can run tools; `allowed-tools` is the gate |
| Claude model → on-chain action | Deploy skill could push contracts to Sepolia (real txs) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-a | Elevation of Privilege | deploy skill auto-invoked by model | mitigate | `disable-model-invocation: true` on deploy/SKILL.md (PLUGIN-03) — only user can trigger |
| T-01-03-b | Tampering | overly-broad tool access on any skill | mitigate | Each `allowed-tools` whitelists specific Bash patterns (`Bash(npx hardhat *)` not bare `Bash`); no skill gets blanket shell access |
| T-01-DESC | Denial of Service (UX) | description bloat past 1,536 chars truncates trigger keywords | mitigate | Frontmatter-length check at end of Task 1; PLAN-04 zod validator enforces invariant |
| T-01-NAME | Tampering | skill `name` collision with reserved/system names | accept | Names `init`, `contract`, `test`, `deploy`, `frontend` are short kebab-case; namespaced under plugin (`/zama-skills:init`); zero ambiguity |
</threat_model>

<verification>
After both tasks:

```bash
# All 5 SKILL.md files exist
for s in init contract test deploy frontend; do
  test -f "plugins/zama-skills/skills/$s/SKILL.md" || { echo "MISSING $s"; exit 1; }
done

# disable-model-invocation ONLY on deploy
for s in init contract test frontend; do
  if grep -q "disable-model-invocation:" "plugins/zama-skills/skills/$s/SKILL.md"; then
    echo "FAIL: $s should not have disable-model-invocation"; exit 1
  fi
done
grep -q "^disable-model-invocation: true$" plugins/zama-skills/skills/deploy/SKILL.md || { echo "FAIL: deploy missing disable-model-invocation"; exit 1; }

# context: fork ONLY on init
for s in contract test deploy frontend; do
  if grep -q "^context: fork$" "plugins/zama-skills/skills/$s/SKILL.md"; then
    echo "FAIL: $s should not have context: fork"; exit 1
  fi
done
grep -q "^context: fork$" plugins/zama-skills/skills/init/SKILL.md || { echo "FAIL: init missing context: fork"; exit 1; }

# allowed-tools on every skill
for s in init contract test deploy frontend; do
  grep -q "^allowed-tools:" "plugins/zama-skills/skills/$s/SKILL.md" || { echo "FAIL: $s missing allowed-tools"; exit 1; }
done

# Combined description + when_to_use ≤ 1536 chars on every skill
for s in init contract test deploy frontend; do
  node -e "
    const m=require('fs').readFileSync('plugins/zama-skills/skills/$s/SKILL.md','utf8');
    const fm=m.match(/^---\\n([\\s\\S]*?)\\n---/)[1];
    const d=(fm.match(/description:\\s*(.+)/m)||[])[1]||'';
    const w=(fm.match(/when_to_use:\\s*(.+)/m)||[])[1]||'';
    if((d+w).length>1536){console.error('$s over cap');process.exit(1);}
  "
done

echo "PLAN-02 invariants OK"
```
</verification>

<success_criteria>
- 5 SKILL.md files at correct paths with folder names matching `name` field
- `name`/`description`/`when_to_use`/`allowed-tools` present on every skill
- `disable-model-invocation: true` ONLY on deploy
- `context: fork` ONLY on init
- Combined description + when_to_use ≤ 1,536 chars on every skill
- PLUGIN-02, PLUGIN-03, PLUGIN-04 satisfied
</success_criteria>

<output>
Implementation only. SUMMARY at end of phase.
</output>
