---
phase: 01-plugin-foundation-ci
plan_id: 01
title: Marketplace + Plugin Manifest
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/marketplace.json
  - plugins/zama-skills/.claude-plugin/plugin.json
  - LICENSE
  - .gitignore
autonomous: true
requirements: [PLUGIN-01]
est_tasks: 3

must_haves:
  truths:
    - "`.claude-plugin/marketplace.json` exists at repo root with `name: zama-skills` + `plugins[0].name: zama-skills` + `plugins[0].source: ./plugins/zama-skills`"
    - "`plugins/zama-skills/.claude-plugin/plugin.json` exists with `name: zama-skills` (no `version` field per dev policy)"
    - "Both JSON files parse as valid JSON and conform to the official Claude Code marketplace + plugin schemas"
    - "Repo has `LICENSE` (MIT) and `.gitignore` covering node_modules, dist, .env"
  artifacts:
    - path: ".claude-plugin/marketplace.json"
      provides: "Marketplace catalog with single plugin entry pointing to ./plugins/zama-skills"
      contains: "\"name\": \"zama-skills\""
    - path: "plugins/zama-skills/.claude-plugin/plugin.json"
      provides: "Plugin manifest"
      contains: "\"name\": \"zama-skills\""
    - path: "LICENSE"
      provides: "MIT license text"
    - path: ".gitignore"
      provides: "Standard Node ignores"
  key_links:
    - from: ".claude-plugin/marketplace.json"
      to: "plugins/zama-skills/.claude-plugin/plugin.json"
      via: "plugins[0].source: \"./plugins/zama-zama-skills\" (relative to marketplace root)"
      pattern: "\"source\":\\s*\"\\./plugins/zama-skills\""
---

<objective>
Create the marketplace catalog and the plugin manifest at the canonical paths required by Claude Code. This is the foundation: without these two JSON files at the right paths, `/plugin marketplace add <repo-url>` fails with "File not found".

Purpose: Make the GitHub repo addressable as a Claude Code plugin marketplace.
Output: `.claude-plugin/marketplace.json` + `plugins/zama-skills/.claude-plugin/plugin.json` + `LICENSE` + `.gitignore`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-plugin-foundation-ci/01-CONTEXT.md
@.planning/phases/01-plugin-foundation-ci/01-RESEARCH.md

# Key path facts (HIGH confidence, from RESEARCH.md):
# - .claude-plugin/marketplace.json MUST be at repo root
# - .claude-plugin/plugin.json MUST be inside each plugin's root
# - plugins[0].source resolves relative to MARKETPLACE ROOT (dir containing .claude-plugin/)
# - Do NOT put skills/ inside .claude-plugin/ — only plugin.json belongs there
# - `..` in source paths is rejected by validator
# - Reserved names: agent-skills, claude-code-marketplace, claude-plugins-official,
#   anthropic-marketplace, anthropic-plugins, claude-code-plugins, knowledge-work-plugins,
#   life-sciences. `zama-skills` is SAFE.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create marketplace.json at repo root</name>
  <files>.claude-plugin/marketplace.json</files>
  <action>
Create directory `.claude-plugin/` at repo root. Write `.claude-plugin/marketplace.json` with this exact shape (per RESEARCH.md Pattern 1, with the GitHub `<owner>` placeholder kept literal — Phase 6 replaces it on first push):

```json
{
  "$schema": "https://json.schemastore.org/claude-code-marketplace",
  "name": "zama-skills",
  "description": "AI agent skills for building confidential dApps with the Zama Protocol (fhEVM)",
  "owner": {
    "name": "Emre Koca",
    "email": "emrekoca2003@gmail.com"
  },
  "plugins": [
    {
      "name": "zama-skills",
      "source": "./plugins/zama-skills",
      "description": "5 skills (init, contract, test, deploy, frontend) for fhEVM dApp scaffolding — context7-aware, anti-hallucination",
      "homepage": "https://github.com/<owner>/zama-skills",
      "repository": "https://github.com/<owner>/zama-skills",
      "license": "MIT",
      "keywords": ["zama", "fhevm", "fhe", "confidential", "ethereum", "solidity"],
      "category": "blockchain"
    }
  ]
}
```

Notes:
- OMIT `version` field on the plugin entry (dev policy: commit SHA = version; pin at submission per RESEARCH.md Pitfall 4 + phase_info instructions).
- Use exact string `./plugins/zama-skills` for `source` — leading `./` is required, `..` is rejected by validator (per D-marketplace-identity + RESEARCH.md anti-patterns).
- `<owner>` placeholder stays as `<owner>` literally; Phase 6 README/submission task replaces it after the GitHub repo URL is finalized.
  </action>
  <verify>
    <automated>node -e "const m=require('./.claude-plugin/marketplace.json'); if(m.name!=='zama-skills') process.exit(1); if(m.plugins[0].source!=='./plugins/zama-skills') process.exit(1); if('version' in m.plugins[0]) process.exit(1); console.log('ok');"</automated>
  </verify>
  <done>
File exists, parses as JSON, `name === "zama-skills"`, `plugins[0].source === "./plugins/zama-skills"`, no `version` field on plugin entry.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create plugin.json inside the plugin directory</name>
  <files>plugins/zama-skills/.claude-plugin/plugin.json</files>
  <action>
Create directories `plugins/zama-skills/.claude-plugin/`. Write `plugins/zama-skills/.claude-plugin/plugin.json` (per RESEARCH.md Pattern 2):

```json
{
  "name": "zama-skills",
  "description": "AI agent skills for building confidential dApps with the Zama Protocol — context7-aware, anti-hallucination",
  "author": {
    "name": "Emre Koca",
    "email": "emrekoca2003@gmail.com"
  },
  "homepage": "https://github.com/<owner>/zama-skills",
  "repository": "https://github.com/<owner>/zama-skills",
  "license": "MIT",
  "keywords": ["zama", "fhevm", "fhe", "confidential", "ethereum", "solidity"]
}
```

CRITICAL — do NOT add a `version` field. Per phase_info: "Omit version field in plugin.json during dev (commit SHA drives versioning); pin only at submission (Phase 6)." This avoids stale-version-cache bugs during the 7-day crunch (RESEARCH.md Pitfall 4).

CRITICAL — do NOT create `plugins/zama-skills/.claude-plugin/skills/` or any other subdirectory inside `.claude-plugin/`. Per RESEARCH.md anti-pattern: "only `plugin.json` belongs there". Skills go at `plugins/zama-skills/skills/` (created in PLAN-02).
  </action>
  <verify>
    <automated>node -e "const p=require('./plugins/zama-skills/.claude-plugin/plugin.json'); if(p.name!=='zama-skills') process.exit(1); if('version' in p) process.exit(1); console.log('ok');"</automated>
  </verify>
  <done>
File exists, parses as JSON, `name === "zama-skills"`, no `version` field, `.claude-plugin/` directory contains ONLY `plugin.json` (no `skills/` or other subdirs).
  </done>
</task>

<task type="auto">
  <name>Task 3: Add LICENSE (MIT) and .gitignore</name>
  <files>LICENSE, .gitignore</files>
  <action>
**LICENSE** — Standard MIT text, copyright `2026 Emre Koca`. Use the canonical MIT template (https://opensource.org/license/mit/).

**.gitignore** — Cover Node + TypeScript + editor + env basics:

```
# dependencies
node_modules/
package-lock.json.backup

# build
dist/
*.tsbuildinfo

# env / secrets
.env
.env.local
.env.*.local

# editor
.DS_Store
.vscode/
.idea/

# coverage / test
coverage/
.nyc_output/

# logs
*.log
npm-debug.log*
```

Note: we DO commit `package-lock.json` (CI uses `npm ci`). We do NOT commit `.env`.
  </action>
  <verify>
    <automated>test -f LICENSE && test -f .gitignore && grep -q "MIT License" LICENSE && grep -q "node_modules/" .gitignore && echo ok</automated>
  </verify>
  <done>
LICENSE has MIT header with year 2026 + name "Emre Koca". .gitignore includes node_modules/, dist/, .env, .DS_Store at minimum.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub repo → user's machine | User runs `/plugin marketplace add <git-url>` — Claude Code clones/caches our repo and reads JSON files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-a | Tampering | `marketplace.json` `source` field | mitigate | Hard-coded `./plugins/zama-skills` (no template substitution); zod validator in PLAN-04 enforces `^\./` prefix and rejects `..` |
| T-01-01-b | Spoofing | marketplace `name` | mitigate | `zama-skills` verified against reserved-name list (RESEARCH.md Pitfall 5); zod validator in PLAN-04 also rejects reserved names |
| T-01-04 | Tampering | `plugin.json` `version` | mitigate | Field intentionally OMITTED during dev so commit SHA drives version resolution; prevents stale-cache bugs |
| T-01-IP | Information Disclosure | LICENSE attribution | accept | Owner email is the user's already-public email (CLAUDE.md `userEmail`); MIT license is intentional public statement |
</threat_model>

<verification>
After all 3 tasks complete:

```bash
# Schema-shape sanity (full zod validation comes in PLAN-04)
node -e "
  const m = require('./.claude-plugin/marketplace.json');
  const p = require('./plugins/zama-skills/.claude-plugin/plugin.json');
  console.assert(m.name === 'zama-skills', 'marketplace.name');
  console.assert(m.plugins[0].name === 'zama-skills', 'plugin entry name');
  console.assert(m.plugins[0].source === './plugins/zama-skills', 'source path');
  console.assert(!('version' in m.plugins[0]), 'no version on entry');
  console.assert(p.name === 'zama-skills', 'plugin.name');
  console.assert(!('version' in p), 'no version on plugin');
  console.log('PLAN-01 invariants OK');
"

# Directory hygiene: nothing else inside .claude-plugin/ besides plugin.json
test \"$(ls plugins/zama-skills/.claude-plugin/)\" = \"plugin.json\" && echo \"plugin .claude-plugin clean\"
test \"$(ls .claude-plugin/)\" = \"marketplace.json\" && echo \"root .claude-plugin clean\"
```
</verification>

<success_criteria>
- Both manifests parse as JSON
- `name`/`source` invariants hold
- No `version` field anywhere
- `.claude-plugin/` directories contain ONLY their respective JSON file
- LICENSE + .gitignore committed
- Ready for PLAN-02 to populate `plugins/zama-skills/skills/`
</success_criteria>

<output>
Implementation only. SUMMARY produced at end of phase by checker.
</output>
