---
phase: 05-reference-example-dapp
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - examples/confidential-token/packages/frontend/package.json
  - examples/confidential-token/packages/frontend/next.config.mjs
  - examples/confidential-token/packages/frontend/tsconfig.json
  - examples/confidential-token/packages/frontend/tailwind.config.ts
  - examples/confidential-token/packages/frontend/postcss.config.mjs
  - examples/confidential-token/packages/frontend/components.json
  - examples/confidential-token/packages/frontend/app/layout.tsx
  - examples/confidential-token/packages/frontend/app/globals.css
  - examples/confidential-token/packages/frontend/app/providers.tsx
  - examples/confidential-token/packages/frontend/lib/wagmi.ts
  - examples/confidential-token/packages/frontend/lib/utils.ts
  - examples/confidential-token/packages/frontend/components/ui/button.tsx
  - examples/confidential-token/packages/frontend/components/ui/card.tsx
  - examples/confidential-token/packages/frontend/components/ui/input.tsx
  - examples/confidential-token/packages/frontend/components/ui/dialog.tsx
  - examples/confidential-token/packages/frontend/components/ui/skeleton.tsx
  - examples/confidential-token/packages/frontend/components/ui/sonner.tsx
  - examples/confidential-token/packages/frontend/.env.local.example
autonomous: true
requirements: [EXAMPLE-03]
must_haves:
  truths:
    - "examples/confidential-token/packages/frontend is a working Next.js 15 App Router app"
    - "shadcn/ui is initialized; Button, Card, Input, Dialog, Skeleton, Sonner toast components exist under components/ui/"
    - "wagmi v2 + viem + RainbowKit are configured for Sepolia only with a single source of truth in lib/wagmi.ts"
    - "Tailwind CSS is wired with dark mode default and Zama brand accent (yellow)"
    - "app/providers.tsx wraps WagmiProvider + RainbowKitProvider + QueryClientProvider"
    - ".env.local.example documents NEXT_PUBLIC_CONTRACT_ADDRESS, NEXT_PUBLIC_SEPOLIA_RPC, NEXT_PUBLIC_RELAYER_URL, NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID"
    - "Files generated by /zama-frontend in Plan 01 (lib/fhe.ts, hooks/useDecrypted.ts, components/EncryptedInput.tsx) remain importable and unmodified"
  artifacts:
    - path: "examples/confidential-token/packages/frontend/app/layout.tsx"
      provides: "Root layout with dark mode, font, providers"
      contains: "Providers"
    - path: "examples/confidential-token/packages/frontend/app/providers.tsx"
      provides: "Wagmi + RainbowKit + QueryClient provider tree"
      contains: "WagmiProvider"
    - path: "examples/confidential-token/packages/frontend/lib/wagmi.ts"
      provides: "Sepolia-only wagmi config + chains export"
      contains: "sepolia"
    - path: "examples/confidential-token/packages/frontend/components.json"
      provides: "shadcn/ui registry config"
      contains: "tailwind"
    - path: "examples/confidential-token/packages/frontend/.env.local.example"
      provides: "Documented env vars for Vercel + local dev"
      contains: "NEXT_PUBLIC_CONTRACT_ADDRESS"
  key_links:
    - from: "examples/confidential-token/packages/frontend/app/providers.tsx"
      to: "examples/confidential-token/packages/frontend/lib/wagmi.ts"
      via: "import config from '@/lib/wagmi'"
      pattern: "lib/wagmi"
    - from: "examples/confidential-token/packages/frontend/components/ui/*"
      to: "lib/utils.ts (cn helper)"
      via: "shadcn standard"
      pattern: "from \"@/lib/utils\""
---

<objective>
Convert the bare `/zama-frontend`-scaffolded `packages/frontend/` into a Next.js 15 App Router app with shadcn/ui, wagmi v2 + viem + RainbowKit, and dark-mode-default Tailwind. Do NOT touch the files /zama-frontend produced (`lib/fhe.ts`, `hooks/useDecrypted.ts`, `components/EncryptedInput.tsx`) — those stay as imported building blocks for Plan 03.

Purpose: Plan 03 (Magic UI screens) needs a working Next.js + shadcn shell to drop pages into. This plan provides ONLY the chassis — no business UI yet.
Output: `pnpm --filter frontend dev` boots a black page with a working "Connect Wallet" button.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reference-example-dapp/05-CONTEXT.md
@CLAUDE.md
@plugins/zama-skills/shared/pinned-versions.json
@plugins/zama-skills/skills/frontend/assets/templates/fhe-wagmi.ts.tpl
@.planning/phases/05-reference-example-dapp/05-01-scaffold-from-skills-PLAN.md

<interfaces>
Pinned package versions (use pnpm add):
- next@15.x (App Router; latest stable 15)
- react@^18 + react-dom@^18 (avoid 19 for relayer-sdk compatibility per CLAUDE.md)
- typescript@^5.9.3 (matches root)
- tailwindcss@^3.4 + postcss + autoprefixer
- shadcn-ui CLI: `pnpm dlx shadcn@latest init` then `pnpm dlx shadcn@latest add button card input dialog skeleton sonner`
- wagmi@^2 + viem@^2 + @tanstack/react-query@^5
- @rainbow-me/rainbowkit@^2 (peer-deps wagmi 2 + viem 2)
- ethers@^6.16.0 (matches CLAUDE.md pin; needed by typechain output if imported)
- @zama-fhe/relayer-sdk@^0.4.2 (already installed by /zama-frontend; verify)
- next-themes@^0.3 (dark mode default)
- sonner@^1 (already added via shadcn)

Chains config (lib/wagmi.ts):
```ts
import { http, createConfig } from 'wagmi';
import { sepolia } from 'wagmi/chains';
import { getDefaultConfig } from '@rainbow-me/rainbowkit';

export const config = getDefaultConfig({
  appName: 'Confidential Demo Token',
  projectId: process.env.NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID!,
  chains: [sepolia],
  ssr: true,
  transports: { [sepolia.id]: http(process.env.NEXT_PUBLIC_SEPOLIA_RPC) },
});
```

Theme: dark mode default via `<html className="dark">` in layout.tsx; Zama accent — define in tailwind.config.ts: `colors.zama: { yellow: '#FFD500', black: '#0A0A0A' }`.

.env.local.example (also doubles as Vercel env doc):
```
# Sepolia RPC (Alchemy / Infura / public)
NEXT_PUBLIC_SEPOLIA_RPC=https://eth-sepolia.g.alchemy.com/v2/<YOUR_KEY>
# WalletConnect Cloud project ID — required by RainbowKit
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=
# Confidential token contract (filled by Plan 04 deploy)
NEXT_PUBLIC_CONTRACT_ADDRESS=
# Zama relayer (Sepolia) — verify at runtime per CLAUDE.md
NEXT_PUBLIC_RELAYER_URL=https://relayer.testnet.zama.cloud
```

Constraint: do NOT delete or modify the files /zama-frontend produced. They live alongside; the Next.js app imports from them via TS path alias `@zama/*` configured in tsconfig.json paths.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 15 App Router + Tailwind + shadcn/ui</name>
  <files>examples/confidential-token/packages/frontend/package.json, next.config.mjs, tsconfig.json, tailwind.config.ts, postcss.config.mjs, components.json, app/layout.tsx, app/globals.css, lib/utils.ts, components/ui/{button,card,input,dialog,skeleton,sonner}.tsx</files>
  <action>
1. Inside `examples/confidential-token/packages/frontend/`:
   - Add Next.js 15 deps: `pnpm add next@15 react@18 react-dom@18 next-themes`. Dev: `pnpm add -D @types/react @types/react-dom @types/node tailwindcss@3 postcss autoprefixer`.
   - Create `next.config.mjs` (default app dir, transpile `@zama-fhe/relayer-sdk` if needed for SSR).
   - Create `tsconfig.json` with `"target": "ES2022"`, paths: `"@/*": ["./*"]`, `"@zama/*": ["./src/*"]` so Plan 03 can import the /zama-frontend output (`@zama/lib/fhe`, `@zama/hooks/useDecrypted`, `@zama/components/EncryptedInput`).
   - Initialize Tailwind: `pnpm dlx tailwindcss init -p`. Configure `tailwind.config.ts` with `content: ["./app/**/*.{ts,tsx}", "./components/**/*.{ts,tsx}", "./src/**/*.{ts,tsx}"]`, `darkMode: "class"`, `colors.zama: { yellow: "#FFD500", black: "#0A0A0A" }`.
   - Run `pnpm dlx shadcn@latest init` (style: default, base color: zinc, RSC: yes, components dir: `components`, utils: `lib/utils`, css var: yes). Then `pnpm dlx shadcn@latest add button card input dialog skeleton sonner`.
   - Author `app/layout.tsx`: `<html lang="en" className="dark">`, body uses Inter font, wraps children with `<Providers>` from Task 2 placeholder. Render `<Toaster richColors />` from sonner.
   - Author `app/globals.css`: shadcn boilerplate + zama yellow CSS var override for `--accent`.
2. Add a temp placeholder `app/page.tsx` rendering `<main className="min-h-screen bg-background text-foreground" />` so the app boots. Plan 03 replaces it.
3. Verify build: `pnpm --filter frontend build` exits 0.
4. Commit: `feat(05): nextjs 15 app router + tailwind + shadcn shell`.
  </action>
  <verify>
    <automated>cd examples/confidential-token/packages/frontend && pnpm build && test -f components/ui/button.tsx && test -f components/ui/card.tsx && test -f components/ui/dialog.tsx && grep -q '"darkMode"\|darkMode:' tailwind.config.ts</automated>
  </verify>
  <done>`pnpm --filter frontend build` succeeds; six shadcn components present; dark mode default; placeholder page renders.</done>
</task>

<task type="auto">
  <name>Task 2: Wire wagmi v2 + viem + RainbowKit (Sepolia-only) + env example</name>
  <files>examples/confidential-token/packages/frontend/lib/wagmi.ts, app/providers.tsx, app/layout.tsx, .env.local.example, components/Connect.tsx</files>
  <action>
1. Install: `pnpm add wagmi viem @tanstack/react-query @rainbow-me/rainbowkit`.
2. Create `lib/wagmi.ts` per `<interfaces>` — Sepolia ONLY, SSR=true, RPC + WalletConnect projectId from env.
3. Create `app/providers.tsx`:
   ```tsx
   'use client';
   import { WagmiProvider } from 'wagmi';
   import { RainbowKitProvider, darkTheme } from '@rainbow-me/rainbowkit';
   import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
   import '@rainbow-me/rainbowkit/styles.css';
   import { config } from '@/lib/wagmi';
   const qc = new QueryClient();
   export function Providers({ children }: { children: React.ReactNode }) {
     return (
       <WagmiProvider config={config}>
         <QueryClientProvider client={qc}>
           <RainbowKitProvider theme={darkTheme()} initialChain={11155111}>
             {children}
           </RainbowKitProvider>
         </QueryClientProvider>
       </WagmiProvider>
     );
   }
   ```
4. Update `app/layout.tsx` to wrap `{children}` in `<Providers>`.
5. Create `components/Connect.tsx`: thin `'use client'` wrapper around `<ConnectButton />` from RainbowKit.
6. Update `app/page.tsx` placeholder to render `<Connect />` so we can smoke test wallet connect.
7. Author `.env.local.example` per `<interfaces>`. Same file is the Vercel env-vars documentation — referenced by Plan 05 README.
8. Verify: `pnpm --filter frontend build` exits 0 (build does not require env vars to be set; missing projectId only fails at runtime).
9. Commit: `feat(05): wagmi v2 + rainbowkit on sepolia + env.local.example`.
  </action>
  <verify>
    <automated>cd examples/confidential-token/packages/frontend && pnpm build && grep -q "sepolia" lib/wagmi.ts && grep -q "WagmiProvider" app/providers.tsx && grep -q "NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID" .env.local.example && grep -q "NEXT_PUBLIC_CONTRACT_ADDRESS" .env.local.example</automated>
  </verify>
  <done>Build green; ConnectButton renders; lib/wagmi.ts uses Sepolia only; env example documents all four NEXT_PUBLIC_* vars.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter frontend build` exits 0.
2. `app/page.tsx` renders Connect button against the dark background.
3. `.env.local.example` lists all four required env vars.
4. /zama-frontend-produced files under `src/` are unmodified (`git diff src/` shows no changes since Plan 01).
</verification>

<success_criteria>
- Next.js 15 App Router app boots
- shadcn/ui six base components installed
- Sepolia-only wagmi config with RainbowKit dark theme
- Env example doubles as Vercel deploy doc (consumed by Plan 05)
- /zama-frontend output untouched and ready for Plan 03 to import via `@zama/*` alias
</success_criteria>

<output>
Create `.planning/phases/05-reference-example-dapp/05-02-SUMMARY.md` listing: shadcn components added, wagmi config decisions, env vars documented, build status, any version pin overrides.
</output>
