"use client";

// Placeholder Providers — Task 2 replaces this with WagmiProvider + RainbowKit
// + QueryClient. Keeping a passthrough here so Task 1 build is green.
export function Providers({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
